rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for system telemetry and authenticated sessions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
